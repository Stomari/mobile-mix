import React from 'react';
import { Text } from 'react-native';

// styles
import TitleStyles from './TitleStyles';

const Title = () => {
  return (
    <Text style={TitleStyles.title}>To Do List</Text>
  );
}

export default Title;